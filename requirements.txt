# Python 3.9+ / Windows
PySide6>=6.7,<7
pydantic>=2.7,<3
pywin32>=306; platform_system == "Windows"
Pillow>=10.0,<12
mss>=10.0,<11
pytesseract>=0.3.13
rapidocr_onnxruntime>=1.4,<2
# quantize=16 is the current Ultralytics precision API used by the detector.
ultralytics>=8.4.90,<9
# NVIDIA CUDA build for the project's Windows environment.
--extra-index-url https://download.pytorch.org/whl/cu126
torch==2.6.0+cu126; platform_system == "Windows"
torchvision==0.21.0+cu126; platform_system == "Windows"
